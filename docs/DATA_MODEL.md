# Data Model

See `README_AGENT.md` for the operational description.

- `planner/profile.yaml` — constraints
- `planner/tasks.yaml` — canonical tasks
- `planner/state.json` — minimal bookkeeping
- `planner/latest/` — overwrite-only outputs
- `reflections/reflections.md` — rolling log
